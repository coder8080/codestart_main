server {
    listen 8080;
    server_name localhost;

    location / {
        proxy_pass http://frontend:8080;
    }

    location /api {
        proxy_pass http://backend:3000;
    }

    location /lessons {
        proxy_pass http://lessons:8080;
        rewrite ^/lessons/(.*) /$1 break;
    }
}